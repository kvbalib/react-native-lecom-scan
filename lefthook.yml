pre-commit:
  parallel: true
  commands:
    lint:
      run: yarn lint --quiet
    types:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx tsc
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
